import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { CheckstatusPage } from './checkstatus';

@NgModule({
  declarations: [
    CheckstatusPage,
  ],
  imports: [
    IonicPageModule.forChild(CheckstatusPage),
  ],
})
export class CheckstatusPageModule {}
